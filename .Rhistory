classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique
} -> departmentList
classListDONE("ZT13")
shannon <- function(x){
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique
}
classListDONE("ZT13")
shannon <- function(x){
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique
}
shannon <- function(x){
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
a <- which(transcriptData$學年 == 104 & transcriptData$學期 == 1 & transcriptData$科目名稱 == 'ZT13')
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique -> departmentList
shannon <- function(x){
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
subsample <- transcriptData [transcriptData$學號 == 'edfeQ7fb8',]
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
subsample %>%
mutate(
及格 = 學期成績 >= 60  )%>%
summarise(
總修習學分數=sum(學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
) -> result
result
subsample %>%
mutate(
及格 = 學期成績 >= 60  )%>%
summarise(
學號= 學號[1],
總修習學分數=sum(學分數),
總實得學分數=sum(subsample$學分數[subsample$及格]),
) -> result1
result1
totalCredits <- function(x){
subsample = transcriptData [transcriptData$學號 == x,]
subsample$及格 = subsample$學期成績 >= 60
總修習學分數=sum(subsample$學分數)
總實得學分數=sum(subsample$學分數[subsample$及格])
學號= x
paste0('學號:',學號,' ','總修習學分數:',總修習學分數,' ','總實得學分數:',總實得學分數)
}
totalCredits("edfeQ7fb8")
transcriptData %>%
mutate(
系別 = str_sub(學號,5,6)
) -> transcriptData
通識 <- which(transcriptData$`必選修類別（必∕選∕通）`=='通')
subsample <- transcriptData[通識,]
subsample$科目名稱 %>%
as.factor%>%
unique -> subjectsName
subjectsName = as.character(subjectsName)
通識科目總表 <- vector('character',length(subjectsName))
通識科目總表 <- subjectsName
通識科目總表
subsample$科目名稱 %>%
as.factor%>%
unique %>%
as.character -> 通識科目總表
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique
}
shannon <- function(x){
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
a <- which(transcriptData$學年 == 104 & transcriptData$學期 == 1 & transcriptData$科目名稱 == 'ZT13')
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique -> departmentList
shannon <- function(x){
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique
}
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
a <- which(transcriptData$學年 == 104 & transcriptData$學期 == 1 & transcriptData$科目名稱 == 'ZT13')
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique -> departmentList
shannon <- function(x){
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
a <- which(transcriptData$學年 == 104 & transcriptData$學期 == 1 & transcriptData$科目名稱 == 'ZT13')
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique -> departmentList
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
subsample <- transcriptData [transcriptData$學號 == 'edfeQ7fb8',]
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
subsample %>%
mutate(
及格 = 學期成績 >= 60  )%>%
summarise(
總修習學分數=sum(學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
) -> result
result
subsample %>%
mutate(
及格 = 學期成績 >= 60  )%>%
summarise(
學號= 學號[1],
總修習學分數=sum(學分數),
總實得學分數=sum(subsample$學分數[subsample$及格]),
) -> result1
result1
totalCredits <- function(x){
subsample = transcriptData [transcriptData$學號 == x,]
subsample$及格 = subsample$學期成績 >= 60
總修習學分數=sum(subsample$學分數)
總實得學分數=sum(subsample$學分數[subsample$及格])
學號= x
paste0('學號:',學號,' ','總修習學分數:',總修習學分數,' ','總實得學分數:',總實得學分數)
}
totalCredits("edfeQ7fb8")
transcriptData %>%
mutate(
系別 = str_sub(學號,5,6)
) -> transcriptData
通識 <- which(transcriptData$`必選修類別（必∕選∕通）`=='通')
subsample <- transcriptData[通識,]
subsample$科目名稱 %>%
as.factor%>%
unique -> subjectsName
subjectsName = as.character(subjectsName)
通識科目總表 <- vector('character',length(subjectsName))
通識科目總表 <- subjectsName
通識科目總表
subsample$科目名稱 %>%
as.factor%>%
unique %>%
as.character -> 通識科目總表
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique
}
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
a <- which(transcriptData$學年 == 104 & transcriptData$學期 == 1 & transcriptData$科目名稱 == 'ZT13')
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique -> departmentList
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
classListDONE("ZT13")
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
department <- classList$系別 %>% as.factor %>% unique
}
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
departmentList <- classList$系別 %>% as.factor %>% unique
}
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
classListDONE("ZT13")
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
departmentList <- classList$系別 %>% as.factor %>% unique
return(departmentList)
}
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
classListDONE("ZT13")
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
departmentList <- classList$系別 %>% as.factor %>% unique
}
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
classListDONE("ZT13")
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList)
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
subsample <- transcriptData [transcriptData$學號 == 'edfeQ7fb8',]
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
subsample %>%
mutate(
及格 = 學期成績 >= 60  )%>%
summarise(
總修習學分數=sum(學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
) -> result
result
subsample %>%
mutate(
及格 = 學期成績 >= 60  )%>%
summarise(
學號= 學號[1],
總修習學分數=sum(學分數),
總實得學分數=sum(subsample$學分數[subsample$及格]),
) -> result1
result1
totalCredits <- function(x){
subsample = transcriptData [transcriptData$學號 == x,]
subsample$及格 = subsample$學期成績 >= 60
總修習學分數=sum(subsample$學分數)
總實得學分數=sum(subsample$學分數[subsample$及格])
學號= x
paste0('學號:',學號,' ','總修習學分數:',總修習學分數,' ','總實得學分數:',總實得學分數)
}
totalCredits("edfeQ7fb8")
transcriptData %>%
mutate(
系別 = str_sub(學號,5,6)
) -> transcriptData
通識 <- which(transcriptData$`必選修類別（必∕選∕通）`=='通')
subsample <- transcriptData[通識,]
subsample$科目名稱 %>%
as.factor%>%
unique -> subjectsName
subjectsName = as.character(subjectsName)
通識科目總表 <- vector('character',length(subjectsName))
通識科目總表 <- subjectsName
通識科目總表
subsample$科目名稱 %>%
as.factor%>%
unique %>%
as.character -> 通識科目總表
classListDONE <- function(X){
a <- which(transcriptData$科目名稱 == X)
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
departmentList <- classList$系別 %>% as.factor %>% unique
}
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
a <- which(transcriptData$學年 == 104 & transcriptData$學期 == 1 & transcriptData$科目名稱 == 'ZT13')
classList <- transcriptData[a,]
classList$系別 <- str_sub(classList$學號,5,6)
classList$系別 %>% as.factor %>% unique -> departmentList1
shannon <- function(x){
studNumAll <- vector("numeric",length(x))
for(i in c(1:length(x))){
x_i <- x[i]
numericalIndexForSubsample <- which(classList$系別==x_i)
subsample_i <- classList[numericalIndexForSubsample,]
studnum_i <- nrow(subsample_i)
studNumAll[[i]] <- studnum_i
}
studNumAll <- studNumAll/sum(studNumAll)
-sum(studNumAll*log(studNumAll))
}
shannon(departmentList1)
